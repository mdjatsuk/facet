variables:
  DOCKER_DRIVER: overlay2
  # Minu konteineri registri aadress AWS-s (Amazon Elastic Container Registry)
  # Asenda MINU_REGISTRI_NIMI
  DOCKER_REGISTRY: 662032419283.dkr.ecr.eu-north-1.amazonaws.com/workout
  DOCKER_TLS_CERTDIR: ""
  DOCKER_HOST: tcp://docker:2375

stages:
  #- build
  - api_test

default:
  image:
    name: docker:28.5.1-alpine3.22
  services:
    - docker:28.5.1-alpine3.22
  before_script:
    - apk add --no-cache curl jq python3 py3-pip
    - pip install --break-system-packages awscli
    - aws ecr get-login-password --region ${AWS_DEFAULT_REGION} | docker login --username AWS --password-stdin ${DOCKER_REGISTRY}
  after_script:
    - docker logout ${DOCKER_REGISTRY}
  tags:
    - itb2203

build:back-end:
  only:
    - master
    - main
  script:
    - docker build -t ${DOCKER_REGISTRY}/back:${CI_COMMIT_SHA} ./Backend/FacetApi/
    - docker push ${DOCKER_REGISTRY}/back:${CI_COMMIT_SHA}
  stage: build

build:front-end:
  only:
    - master
    - main
  script:
    - docker build -t ${DOCKER_REGISTRY}/front:${CI_COMMIT_SHA} ./Frontend/
    - docker push ${DOCKER_REGISTRY}/front:${CI_COMMIT_SHA}
  stage: build

api_test:
  image:
    name: docker:28.5.1-alpine3.22
    entrypoint: ["/bin/sh", "-c"]
  services:
    - docker:28.5.1-alpine3.22
  only:
    - master
    - main
  script:
    - cd ApiTests
    - docker compose up --abort-on-container-exit --exit-code-from newman
    - docker compose down -v
  stage: api_test
  